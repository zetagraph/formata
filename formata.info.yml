name: formata
description: Drupal 8 Theme
core: 8.x
engine: twig
version: 1.0
type: theme
screenshot: img/screenshot.png

stylesheets:
  all:
  - css/style.css

# Remove a CSS file:
stylesheets-remove:
  - normalize.css

regions:
   content: 'Content'
   sidebar: 'Sidebar'
   footer: 'Footer'
